###################################
# MKRX   - network unix clones
###################################
# con -> usb -> $KEY -> unmount -> ssh -> ANONI -> usb
# TODO ############################
rm chdir 

server
HIVE

notary
forgien $KEY
ntp
proxy

KERN
start_if.
login.conf
src.conf

clan anoni assignment
devfs host/jail ruleset
pure c code 
usb-install-release
capisum groups
conf
chmod general hardening
fix ports/mod install

# USB #############################
BANK
	authorized_keys
	net	node ip4 list
	host/	host $KEY dumps
KERN
	README
	bin/
		host programs
	jbin/
		jail programs
	conf/
		host conf files
	jconf/
		jail conf files
	install/
		host install proceedure
	jinstall/
		jail install proceedure
LOG
	misc/
META
	NAM
	PAT
	SIZ
	ENC
# PREP ############################
# load usb with authorized_keys
# INSTALL PROCEEDURE ##############
# set bios boot from usb
# boot FreeBSD install memstick
# --- REBOOT ---
# zfs import usb 
# /bin/sh /usb/KERN/install/1.INIT
# perl /usb/KERN/install/2.PORT /usb
# perl /usb/KERN/install/3.WORLD /usb
# vim rc.conf && vim pf.conf && vim jail.conf
# rm -r /usr/obj
# cd /usr/src/
# make -j2 buildkernel KERNCONF=KERN
# make installkernel KERNCONF=KERN
# rm -r /usr/obj
# make -j2 buildworld
# make installworld
# make installworld DESTDIR=/usr/ANONI
# make distribution DESTDIR=/usr/ANONI
# cp -R /usr/local/ /usr/ANONI/usr/local/
# zpool export usb
# zpool import usb -o altroot=/usr/ANONI/mnt usb
##### cp /bin/* /usr/ANONI 
# jail -c anoni
# jexec 1 /bin/csh
# perl usb/KERN/jinstall/1.WORLD /usb
# perl usb/KERN/jinstall/2.ANONI /usb hostname
# vim rc.conf
# zpool export usb
# --- REBOOT ---
# zpool import usb
# perl usb/KERN/install/4.POSTJAIL
# perl usb/KERN/install/5.MAP
###################################
