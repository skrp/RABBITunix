###################################
# MKRX   - network unix clones
###################################
# unix clones allow for uniform standardization keystone of network security
# OTOPSK allows next-level obfuscation for anonymous usr traffic

# usr takes a FreeBSD 10.3 installer usb
# inserts into an AMD64 computer
# installs FreeBSD 10.3 GENERIC
# sculpts new kernel via commands below
# populates usb with OTOPSK access-keys $KEY for the new host
# usr trades $KEY amongst network for access to other hosts

# CLAN nfs unix groups allow persistent data accross ANONI sessions

# norm usr via ssh ANONI jail access
# heir usr via ssh ANONI jail access only way to access host 

# Only way root 
# heir $KEY -> ssh ANONI heir -> RABBIT $KEY -> ssh host RABBIT -> su root 

# Anonymous - all usr accounts deleted after first access
# Secure    - usb pre-shared keys counter man-in-the-middle
# TERMS ###########################
# con    - physical host console user
# norm   - jail ssh user
# heir   - jail ssh user able to access host
# lord   - host ssh capiscum super-user
# $KEY   - rsa encrypted access-key
# OTOPSK - one time only pre shared key 
# RABBIT - alarm authenticated-wheel hole to host root
# ANONI  - external-anonymous access shell interface
# WORLD  - unix nfsv4 CLAN filesystem
# CLAN   - unix network groups
# USB #############################
BANK
	net	node ip4 list
	host/	host dumps
	  node/ host $KEY
	   usr/ known_host(id_rsa.pub) id_rsa 
KERN
	README
	bin/
		host programs
	jbin/
		jail programs
	conf/
		host conf files
	jconf/
		jail conf files
	install/
		host install proceedure
	jinstall/
		jail install proceedure
LOG
	misc/
META
	NAM
	PAT
	SIZ
	ENC
# INSTALL PROCEEDURE ##############
#### set bios boot from usb
# boot FreeBSD install memstick
#### press enter
#### select Shell
# camcontrol devlist
#### find the drive
# dd if=/dev/zero of=/dev/drive bs=1M
#### wait a minute
# ctrl-c
# exit
#### select Install
#### type hostname
#### tick via spacebar to only have selected: doc src
#### select Auto (ZFS)
#### press enter
#### press enter
#### tick drive 
#### select YES
#### root pass = root
#### select Cancel
#### select NO
#### whatever
#### whatever
#### select YES
#### only tick dumpdev
#### select NO
#### select exit
#### select NO
#### select Reboot
# --- REBOOT ---
# zfs import usb 
# cd /usb/KERN/install/
# /bin/sh /usb/KERN/install/1.INIT
# perl /usb/KERN/install/2.PORT /usb
# perl /usb/KERN/install/3.WORLD /usb node
# vim /etc/rc.conf && vim /etc/pf.conf && vim /etc/jail.conf && vim /etc/pf_net
# rm -r /usr/obj
# cd /usr/src/
# make -j2 buildkernel KERNCONF=KERN
# make installkernel KERNCONF=KERN
# rm -r /usr/obj
# make -j2 buildworld
# make installworld
# make installworld DESTDIR=/usr/ANONI
# make distribution DESTDIR=/usr/ANONI
# cp -R /usr/local/ /usr/ANONI/usr/local/
# zpool export usb
# zpool import usb -o altroot=/usr/ANONI/mnt usb
##### cp /bin/* /usr/ANONI 
# jail -c anoni
# jexec 1 /bin/csh
# perl usb/KERN/jinstall/1.WORLD /usb
# perl usb/KERN/jinstall/2.ANONI /usb hostname
# vim rc.conf
# zpool export usb
# --- REBOOT ---
# zpool import usb
# perl usb/KERN/install/4.POSTJAIL
# perl usb/KERN/install/5.MAP
###################################
