#!/bin/sh
###########################
# STEP ONE : perl
###########################
tar -xC /usr/ports/ -f ../distfiles/perl.tar
cd /usr/ports/perl/ && ./configure && make && make install
###########################
